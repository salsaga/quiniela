from django.apps import AppConfig


class QuinielaConfig(AppConfig):
    """Configuración de la app principal de la quiniela."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "quiniela"
